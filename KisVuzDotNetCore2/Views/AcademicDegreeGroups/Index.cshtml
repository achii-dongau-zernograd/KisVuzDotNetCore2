@model IEnumerable<KisVuzDotNetCore2.Models.AcademicDegreeGroup>

@{
    ViewData["Title"] = "Группы учёных степеней";
}

<h2>Группы учёных степеней</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.AcademicDegreeGroupName)
                </th>
                <th>
                    Действия
                </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AcademicDegreeGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AcademicDegreeGroupId">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.AcademicDegreeGroupId">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.AcademicDegreeGroupId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
