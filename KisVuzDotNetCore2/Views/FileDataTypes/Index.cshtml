@model IEnumerable<KisVuzDotNetCore2.Models.FileDataType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FileDataTypeName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FileDataTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FileDataTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FileDataTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FileDataTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
