@model KisVuzDotNetCore2.Models.Education.EduLevel

@{
    ViewData["Title"] = "Уровень образования. Редактирование";
}

<h2>Редактирование</h2>

<h4>Уровень образования</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EduLevelId" />
            <div class="form-group">
                <label asp-for="EduLevelName" class="control-label"></label>
                <input asp-for="EduLevelName" class="form-control" />
                <span asp-validation-for="EduLevelName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-default">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
