@model IEnumerable<KisVuzDotNetCore2.Models.EduLevelGroup>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.EduLevelGroupName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EduLevelGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EduLevelGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EduLevelGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EduLevelGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
