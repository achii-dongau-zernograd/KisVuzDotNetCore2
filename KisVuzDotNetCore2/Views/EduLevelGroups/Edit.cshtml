@model KisVuzDotNetCore2.Models.EduLevelGroup

@{
    ViewData["Title"] = "Редактировать группу уровней образования";
}

<h2>Редактировать группу уровней образовани</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EduLevelGroupId" />
            <div class="form-group">
                <label asp-for="EduLevelGroupName" class="control-label"></label>
                <input asp-for="EduLevelGroupName" class="form-control" />
                <span asp-validation-for="EduLevelGroupName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-default">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
