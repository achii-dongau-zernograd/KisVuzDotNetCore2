@model IEnumerable<KisVuzDotNetCore2.Models.Education.DisciplineName>

@{
    ViewData["Title"] = "Дисциплины";
}

<h2>Дисциплины</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DisciplineNameName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DisciplineNameName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DisciplineNameId">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.DisciplineNameId">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.DisciplineNameId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
