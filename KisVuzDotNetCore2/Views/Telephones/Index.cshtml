@model IEnumerable<KisVuzDotNetCore2.Models.Struct.Telephone>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TelephoneNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TelephoneComment)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TelephoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelephoneComment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TelephoneId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TelephoneId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TelephoneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
