@model IEnumerable<KisVuzDotNetCore2.Models.Struct.StructKaf>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StructFacultet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StructSubvision)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StructFacultet.StructFacultetId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StructSubvision.StructSubvisionId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StructKafId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StructKafId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StructKafId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
