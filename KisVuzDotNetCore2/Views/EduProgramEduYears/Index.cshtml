@model IEnumerable<KisVuzDotNetCore2.Models.Education.EduProgramEduYear>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EduProgram)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EduYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EduProgram.EduProgramId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EduYear.EduYearId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EduProgramEduYearId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EduProgramEduYearId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EduProgramEduYearId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
