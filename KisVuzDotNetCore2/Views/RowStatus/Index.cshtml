@model IEnumerable<KisVuzDotNetCore2.Models.RowStatus>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.RowStatusName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RowStatusName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RowStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RowStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RowStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
