@model KisVuzDotNetCore2.Models.Struct.Email

@{
    ViewData["Title"] = "Редактировать";
}

<h2>Редактировать</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EmailId" />
            <div class="form-group">
                <label asp-for="EmailValue" class="control-label"></label>
                <input asp-for="EmailValue" class="form-control" />
                <span asp-validation-for="EmailValue" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmailComment" class="control-label"></label>
                <input asp-for="EmailComment" class="form-control" />
                <span asp-validation-for="EmailComment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-default">Отмена</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
