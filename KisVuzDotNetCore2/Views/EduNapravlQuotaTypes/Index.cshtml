@model IEnumerable<KisVuzDotNetCore2.Models.Abitur.EduNapravlQuotaType>

@{
    ViewData["Title"] = "Index";
}

<h2>Направления подготовки - Типы квот приёма</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EduNapravlId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuotaTypeId)
            </th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EduNapravl.GetEduNapravlName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuotaType.QuotaTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EduNapravlQuotaTypeId" class="btn btn-primary">Редактировать</a>
                <a asp-action="Delete" asp-route-id="@item.EduNapravlQuotaTypeId" class="btn btn-danger">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
