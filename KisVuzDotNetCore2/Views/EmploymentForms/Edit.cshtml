@model KisVuzDotNetCore2.Models.Common.EmploymentForm

@{
    ViewData["Title"] = "Редактирование";
}

<h2>Редактирование формы занятости</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EmploymentFormId" />
            <div class="form-group">
                <label asp-for="EmploymentFormName" class="control-label"></label>
                <input asp-for="EmploymentFormName" class="form-control" />
                <span asp-validation-for="EmploymentFormName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-default">Отмена</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
