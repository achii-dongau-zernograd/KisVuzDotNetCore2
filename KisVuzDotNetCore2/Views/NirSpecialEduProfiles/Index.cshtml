@model IEnumerable<KisVuzDotNetCore2.Models.Nir.NirSpecialEduProfile>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EduProfile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NirSpecial)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EduProfile.EduProfileId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NirSpecial.NirSpecialId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NirSpecialEduProfileId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NirSpecialEduProfileId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NirSpecialEduProfileId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
