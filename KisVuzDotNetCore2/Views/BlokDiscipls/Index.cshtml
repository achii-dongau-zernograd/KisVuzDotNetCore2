@model IEnumerable<KisVuzDotNetCore2.Models.Education.BlokDiscipl>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BlokDisciplName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EduPlan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BlokDisciplName.BlokDisciplNameId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EduPlan.EduPlanId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BlokDisciplId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BlokDisciplId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BlokDisciplId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
