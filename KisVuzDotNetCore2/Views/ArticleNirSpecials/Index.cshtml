@model IEnumerable<KisVuzDotNetCore2.Models.Nir.ArticleNirSpecial>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Article)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NirSpecial)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Article.ArticleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NirSpecial.NirSpecialId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArticleNirSpecialId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArticleNirSpecialId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArticleNirSpecialId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
