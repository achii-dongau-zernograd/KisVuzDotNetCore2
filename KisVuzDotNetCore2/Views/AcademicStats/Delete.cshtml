@model KisVuzDotNetCore2.Models.AcademicStat

@{
    ViewData["Title"] = "Удалить учёное звание?";
}

<h2>Удалить учёное звание?</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AcademicStatName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AcademicStatName)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="AcademicStatId" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-default">Отмена</a>
    </form>
</div>
